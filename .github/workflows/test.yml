name: test
on:
  push:
    branches:
      - "**"
env:
  MYSQL_VERSION: '8.0.3'
  DB_USER: 'root'
  DB_PASS: 'root'
  DB_HOST: 'localhost'
  DB_PORT: 3306
  DB_NAME: 'test'
jobs:
  test:
    name: test
    strategy:
      matrix:
        go-version: [1.15.x]
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: mirromutth/mysql-action@v1.1
        with:
          mysql version: ${MYSQL_VERSION}
          mysql root password: ${DB_PASS}
      - uses: actions/checkout@v2
      - name: install Go
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go-version }}
      - name: migrating
        run: go run src/cmd/blog/main.go
      - name: testing
        run: go test -race -v ./src/...
